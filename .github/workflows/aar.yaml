name: Build Android AAR (Optimized)

on: [workflow_dispatch]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        # We need the full history and submodules for ZeroTier core
        fetch-depth: 0
        submodules: 'recursive'

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Install and Configure Android NDK & CMake
      run: |
        # Install the EXACT NDK and CMake versions requested by the project
        sdkmanager "ndk;25.1.8937393" "cmake;3.22.1"
        
        NDK_PATH="$ANDROID_SDK_ROOT/ndk/25.1.8937393"
        CMAKE_PATH="$ANDROID_SDK_ROOT/cmake/3.22.1/bin"
        
        # Ensure the directory exists
        mkdir -p pkg/android
        
        # local.properties helps Gradle find everything
        echo "sdk.dir=$ANDROID_SDK_ROOT" > pkg/android/local.properties
        echo "ndk.dir=$NDK_PATH" >> pkg/android/local.properties
        # Tell Gradle exactly where the requested CMake is
        echo "cmake.dir=$ANDROID_SDK_ROOT/cmake/3.22.1" >> pkg/android/local.properties
        
        # Add CMake to the system PATH for this job
        echo "$CMAKE_PATH" >> $GITHUB_PATH
        
        # Export environment variables for the build script
        echo "ANDROID_NDK_HOME=$NDK_PATH" >> $GITHUB_ENV
        echo "NDK_PATH=$NDK_PATH" >> $GITHUB_ENV

    - name: Build libzt (Direct Gradle)
      run: |
        # Ensure submodules are ready
        git submodule update --init --recursive
        
        cd pkg/android
        chmod +x gradlew
        # Run the build again; now that NDK versions match, it should proceed to compile
        ./gradlew assembleRelease --stacktrace || echo "Gradle build failed, but checking for outputs..."

    - name: Locate AAR and Debug
      run: |
        mkdir -p final_output
        # Find any AARs in the pkg/android directory
        find pkg/android -name "*.aar" -exec cp {} final_output/ \;
        echo "Files collected for upload:"
        ls -R final_output

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: libzt-android-v8a
        path: final_output/
        if-no-files-found: error
